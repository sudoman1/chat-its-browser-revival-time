const page=document.getElementById('page');const viewport=document.querySelector('.page-viewport');const root=document.documentElement;function fitPage(){if(!page||!viewport)return;root.style.setProperty('--page-scale','1');const viewportWidth=viewport.clientWidth;const viewportHeight=viewport.clientHeight;const pageWidth=page.offsetWidth;const pageHeight=page.offsetHeight;const scale=Math.min(viewportWidth/pageWidth,viewportHeight/pageHeight,1);root.style.setProperty('--page-scale',scale.toString())}
window.addEventListener('resize',fitPage);window.addEventListener('load',fitPage)
